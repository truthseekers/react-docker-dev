version: "3.7"

services:
  ui:
    container_name: react
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - "./ui:/app"
    ports:
      - 3001:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
